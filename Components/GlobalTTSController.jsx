/**
 * Global TTS Controller - DISABLED
 * 
 * Autoplay on route changes is disabled.
 * Users must click "Enable Voice" button to use TTS.
 * No automatic speaking on page load.
 */
export function GlobalTTSController() {
  // Completely disabled - no autoplay
  return null;
}

export default GlobalTTSController;
